/**
 * SOLO PARA PRUEBAS LOCALES — BORRAR ESTE ARCHIVO ANTES DE PASAR A PRODUCCIÓN.
 *
 * Pega aquí un access_token válido para probar el MFE sin el flujo OAuth del shell
 * (p. ej. copiado del localStorage del root ya autenticado, o el que uses en Postman).
 * Si queda vacío, el interceptor usa el token del localStorage como siempre.
 *
 * Para eliminarlo: borra este archivo y las 2 líneas marcadas con "TEST TOKEN"
 * en auth.interceptor.ts.
 */
export const DEV_TOKEN = '';
